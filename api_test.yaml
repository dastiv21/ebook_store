- config:
    testset: "Sample Test Set"
    url: "http://localhost:8000/api/"
    timeout: 5

- test:
    name: "Get list of available books"
    request:
        url: "books/"  # Corrected the relative URL
        method: GET
